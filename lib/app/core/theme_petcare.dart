import 'package:flutter/material.dart';

class ThemePetCare {
  static theme() {
    return ThemeData(
      primaryColor: Color(0xFFFFBABA),
      primaryColorDark: Color(0xFFE22727),
      primaryColorLight: Color(0xFFFCEEEE)
    );
  }
}
